# The number of trials of a fuzzer-benchmark pair.
trials: 1

# The amount of time in seconds that each trial is run for.
# 1 day = 24 * 60 * 60 = 86400
#max_total_time: 300
max_total_time: 900
#max_total_time: 1800
#max_total_time: 3600
#max_total_time: 43200
#max_total_time: 86400


# The location of the docker registry.
# FIXME: Support custom docker registry.
# See https://github.com/google/fuzzbench/issues/777
docker_registry: gcr.io/fuzzbench

# The local experiment folder that will store most of the experiment data.
# Please use an absolute path.
#experiment_filestore: /home/ubuntu/green_fuzzing_evaluation/experiment-data
experiment_filestore: /home/spino/ba/green_fuzzing_evaluation/experiment-data

# The local report folder where HTML reports and summary data will be stored.
# Please use an absolute path.
#report_filestore: /home/ubuntu/green_fuzzing_evaluation/report-data
report_filestore: /home/spino/ba/green_fuzzing_evaluation/report-data

# Flag that indicates this is a local experiment.
local_experiment: true

snapshot_period: 60
#snapshot_period: 300
#snapshot_period: 900