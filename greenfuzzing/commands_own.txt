Sitzung starten: tmux new -s sitzungsnamen
Programm starten
Sitzung verlassen: Str + b, dann d
Alle laufenden Sitzungen anzeigen: tmux ls
Zur laufenden Sitzung verbinden: tmux attach -t sitzungsnamen
Sitzung beenden: exit oder Logout

tmux list-session: Zeigt alle aktiven Sitzungen an
tmux kill-session -t name: Beendet die Sitzung mit dem Namen
tmux rename-session -t alterName neuerName



PYTHONPATH=. python3 experiment/run_experiment.py \
--experiment-config experiment-config.yaml \
--benchmarks freetype2_ftfuzzer bloaty_fuzz_target \
--experiment-name testeval \
--fuzzers afl libfuzzer

source .venv/bin/activate

PYTHONPATH=. python3 experiment/run_experiment.py -c experiment-config.yaml -b freetype2_ftfuzzer bloaty_fuzz_target -e testimpl -f afl libfuzzer -a > greenfuzzing/out.txt



htop
df -h
free -h

docker system df
docker ps -a
docker stop Name
docker rm Name
docker image prune -f
docker image prune -a -f
docker builder prune -f



tmux kill-session -t 0
docker stop $(docker ps -q)
sudo rm -rf experiment-data/ report-data/ config


scp cispa-vm:~/green_fuzzing_evaluation/greenfuzzing/local.db .